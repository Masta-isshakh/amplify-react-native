version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 18
        - nvm use 18
        - corepack enable
        - npm ci
    build:
      commands:
        - echo "✅ No web build for React Native - skipping"
  artifacts:
    baseDirectory: .
    files:
      - amplify_outputs.json
  cache:
    paths:
      - node_modules/**/*
